동적인 배열을 랜더링 할 때 자바스크립트는 map함수를 사용합니다.
map함수는 배열안에 있는 각 원소를 변환하여 새로운 배열을 만들어줍니다.
리액트에서 동적인 배열을 랜더링해야 할 때는 이 함수를 사용하는데 
각 원소마다 유니크한 key를 줘야합니다.
      {users.map(user => (
        <User user={user} key={user.id} />
      ))}

키값이 따로 없다면 고유의 index를 사용하면 되지만 매우 비효율적입니다.
<div>
  {users.map((user, index) => (
    <User user={user} key={index} />
  ))}
</div>

리액트는 리랜더링 할 때(배열의 구조가 바뀔 때 삽입 삭제 추가 등) 키값이 없으면
각각의 모든 인덱스의 배열 값을 변경 해주고 맨 아래에 새로 추가를 합니다.

다만 키값이 따로 존재할 경우 기존의 값은 수정되지 않고 원하는 위치에 값만 수정하거나
추가 삭제가 이뤄저 훨씬 효율적입니다.
